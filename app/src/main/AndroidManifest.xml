<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"

    package="com.chilloutrecords">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />
    <uses-permission android:name="com.google.android.c2dm.permission.RECEIVE" />

    <permission
        android:name="com.chilloutrecords.permission.C2D_MESSAGE"
        android:protectionLevel="signature" />
    <uses-permission android:name="com.chilloutrecords.permission.C2D_MESSAGE" />

    <uses-feature
        android:glEsVersion="0x00020000"
        android:required="true" />

    <application
        android:name="com.chilloutrecords.utils.ChilloutRecords"
        android:allowBackup="false"
        android:screenOrientation="portrait"
        android:windowSoftInputMode="stateHidden"
        android:label="@string/app_name"
        android:theme="@style/AppTheme"
        android:icon="${icon}"
        android:roundIcon="${icon_round}">

        <!--START UP PAGES ======================================================================-->
        <activity
            android:name=".activities.SplashScreenActivity"
            android:screenOrientation="portrait">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:host="chilloutrecords.com"
                    android:scheme="https" />
            </intent-filter>

        </activity>

        <activity
            android:name=".activities.StartUpActivity" />


        <!-- MAIN PAGES =========================================================================-->
        <activity android:name=".activities.ParentActivity" />

        <activity
            android:name=".activities.ParentToolBarActivity"
            android:launchMode="singleTask" />

        <activity
            android:name=".activities.GeneralListActivity"
            android:launchMode="singleTask" />

        <activity
            android:name=".activities.ArtistDetailsActivity"
            android:launchMode="singleTask" />

        <activity
            android:name=".activities.TextActivity"
            android:launchMode="singleTask" />

        <activity
            android:name=".activities.MyAccountActivity"
            android:launchMode="singleTask" />

        <activity
            android:name=".activities.MusicActivity"
            android:launchMode="singleTask" />

        <activity
            android:name=".activities.VideoPlayerActivity"
            android:launchMode="singleTask"
            android:screenOrientation="landscape"
            android:windowSoftInputMode="stateHidden" />

        <!--CLOUD MESSAGING SERVICES ============================================================-->
        <service android:name=".services.MessagingService">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

    </application>

</manifest>